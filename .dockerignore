__pycache__
*.pyc
.env
.env.*
.git
.gitignore
.pytest_cache
*.egg-info
node_modules
frontend
data
# NOTE: prisma/ and hiker_ingestion/ are NOT excluded - the Dockerfile
# explicitly COPYs both into the image (schema docs + the ingestion
# package); excluding them here broke every container build until now.